---
spec: developer-experience
created: 2026-03-07
generated: auto
---

# Progress: Developer Experience Improvements

## Status
Phase: execution
Task: 0 / 12

## Learnings
- `grapheme` PyPI package is NOT installed; grapheme-cluster truncation must use stdlib `unicodedata` only.
- Variation selectors: `✖️` is U+2716 + U+FE0F (len=2), `✖` is U+2716 (len=1). `difflib.get_close_matches` with cutoff=0.6 should catch these since SequenceMatcher works at codepoint level and they share the base codepoint.
- `ParseError` currently takes 3 positional args; adding `func_name` as 4th keyword-only param is safe for all existing call sites.
- `VMError` currently takes 2 positional args; `source`/`func_name` added as keyword defaults.
- `current_func.name` IS available at parser line 148 — confirmed by reading the parse() function body.
- `inst.source` IS set on every Instruction — raw stripped source line, confirmed at parser.py:158.
- REPL: sharing VM state via direct attribute assignment (`vm.stack = shared_list`) works because Python list references are mutable.
- REPL: must reset `vm.halted`, `vm.call_stack`, `vm.steps` after each instruction execution to prevent state bleed.
- Project has no external runtime deps beyond stdlib; must stay that way.
- 298 tests at 94% coverage — new tests should push coverage higher.
